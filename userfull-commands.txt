// Unitialize a new directory from the genesis jsom
geth init .\genesis.json --datadir mychaindata

// Initialize blockchain
 geth --datadir mychaindata --nodiscover

// Attach to another geth process
geth attach

// Checks if we have accounts (they are located in muychaindata keystore)
eth.accounts

// Display usefull functions
personal

// Create new account:
personal.newAccount("password")

// Add some ether into the account by mining

// MINING:

// Make sure that your account is visible
eth.coinbase

// If not visible just
miner.setEtherbase(eth.accounts[0]);

// miner start in 4 cores
miner.start(4);

// Check how many ether do we have
web3.eth.getBalance(eth.accounts[0]);

// In etherum
web3.fromWei(eth.getBalance(eth.accounts[0]), "ether");
web3.fromWei(eth.getBalance(eth.accounts[0]), "finney");